'use client';

// Re-export the main page component to handle the /reset-password route
// The main logic in app/page.tsx handles view switching based on auth callback
import AppPage from '../page';

export default AppPage;
